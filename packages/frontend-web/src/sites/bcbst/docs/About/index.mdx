import { Canvas, Meta, Unstyled } from '@storybook/addon-docs/blocks';
import TokenExample from './TokenExample';
import '../../../../global/styles/sb-markdown.scss';

<Meta title="Design System/About" />
<Unstyled>
# BCBST

This storybook comprises the core components which make up the monorepo.

Each Story represent and example of the HTML mark up for that component in a specific state.

Each component SCSS file as well as a compile version of the CSS is available via `@bsbst/frontend-web`.

## All Tokens

<TokenExample />

</Unstyled>