import { Canvas, Meta, Unstyled } from '@storybook/blocks';

<!-- if you need to use a component to repeat through tokens, include the below component -->
<!-- import TokenExample from './TokenExample'; -->

<Meta title="{{pascalCase name}}" />
<Unstyled>

<!-- Add and example of how the tokens can be used -->
<!-- <pre>
```css
.acs-test-component {
    --_private: var(--color-grey-light24); // set a private variable
    color: var(--color-grey-dark32); // directly use a root variable
}
```
</pre> -->

<!-- <pre>
```js
import {
  COLOR_BRAND_BLUE,
} from '../../../data/tokens.es6';
```

## Presentation -->
<!-- <TokenExample type="presentation" /> -->

</Unstyled>